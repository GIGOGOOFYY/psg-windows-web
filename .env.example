# Copy this file to ".env" for your own reference (it is gitignored, never pushed).
# The REAL values go into Cloudflare Pages → Settings → Environment variables.
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-public-key...
